apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: zack-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
spec:
  rules:
  - host: zack.k8s.halyard.app
    http:
      paths:
      - backend:
          serviceName: zack
          servicePort: https
  tls:
  - hosts:
    - zack.k8s.halyard.app
    secretName: zack
